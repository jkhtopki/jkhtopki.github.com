<div class="entry">
	<h2>
		{% if detail %}
			{{ post.title }}
		{% else %}
			<a href="{{ post.url }}">{{ post.title }}</a>
		{% endif %}
	</h2>
	
	<div class="entry-date">{{ post.date | date:'%b <div>%d</div> %Y' | replace:'>0','>' }}</div>
	
	<div class="entry-contents">
		{% if detail %}
			{{ content }}
		{% else %}
			{{ post.content }}
		{% endif %}
	</div>
	
	<div class="entry-footer">
		<p>
			Опубликовано {{ site.author.name }} {{ post.date | date:'%A, %B %d, %Y' | replace:' 0',' ' }}
		</p>
		<p>
			Категории
			{{ post.categories | array_to_sentence_string | capitalize }}
			{% unless post.tags == empty %}
				и метки
				{{ post.tags | array_to_sentence_string }}
			{% endunless %}
		</p>
	</div>
</div>
